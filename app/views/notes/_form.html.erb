
<form action="<%= notes_url %>" method="post">
  <%= auth_token %>

  <label>Title
    <input type="string" name="note[title]" value="">
  </label>
  <br>
  <textarea name="note[body]" rows="8" cols="40"></textarea>
  <br>
  <input type="hidden" name="note[track_id]" value="<%= track.id %>">
  <button>Submit</button>
</form>
